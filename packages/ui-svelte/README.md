# ui-svelte

Shared Svelte UI package.

Current contents:

- reusable UI components extracted from `apps/web-main/src/lib/components/ui`
- shared UI utilities in `src/lib/utils.ts`

This package is currently consumed through local source aliases from app code.
