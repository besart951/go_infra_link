# Packages

Shared reusable frontend packages live here.

Current shared packages:

- `ui-svelte`
- `theme`
- `i18n`

Planned shared packages:

- `typescript-sdk`

This folder is intentionally prepared before extraction work starts, so shared code can move here incrementally without another repository reshape.
