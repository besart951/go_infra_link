# Apps

Runnable applications live here.

Current applications:

- `web-main`: the current Svelte web frontend

Reserved placeholders:

- `client-next`: reserved for the second frontend/client application

Notes:

- `client-next` is intentionally technology-neutral for now. It can later become React, Flutter, Tauri, Electron, or another stack without changing the backend/API ownership model.
