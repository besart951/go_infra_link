meta {
  name: List Phase Permissions
  type: http
  seq: 2
}

get {
  url: {{base_url}}/api/v1/phase-permissions?phase_id={{phase_id}}
  body: none
  auth: none
}

headers {
  X-CSRF-Token: {{csrf_token}}
}

params:query {
  phase_id: {{phase_id}}
}
