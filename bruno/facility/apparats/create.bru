meta {
  name: Create Apparat
  type: http
  seq: 55
}

post {
  url: {{baseUrl}}/api/v1/facility/apparats
  body: json
  auth: none
}

headers {
  Content-Type: application/json
  X-CSRF-Token: {{csrf_token}}
}

body:json {
  {
    "short_name": "APP-1",
    "name": "Apparat 1",
    "description": "Example description"
  }
}

script:post-response {
  const data = res.body;
  if (data && data.id) {
    bru.setVar('apparat_id', data.id);
  }
}
